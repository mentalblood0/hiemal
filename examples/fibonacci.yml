with:
  functions:
    fibonacci::
      if:
        is sorted:
          - _
          - 1
      then: _
      else:
        sum: # => Array(Number) = Array(Tfibonacci) => Number = Tfibonacci
          # Array(Tfibonacci)
          - fibonacci:: # Tfibonacci
              sum: # Number
                - _
                - -1
          - fibonacci:: # Tfibonacci
              sum: # Number
                - _
                - -2
compute:
  fibonacci:: 10
