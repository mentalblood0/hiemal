with:
  functions:
    fibonacci::
      if:
        is sorted: [_, 1]
      then: _
      else:
        sum:
          - fibonacci::
              sum: [_, -1]
          - fibonacci::
              sum: [_, -2]
compute:
  fibonacci:: 10
